'use strict';

/**
 * establishment-image service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::establishment-image.establishment-image');
