'use strict';

/**
 * accommodation-type service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::accommodation-type.accommodation-type');
