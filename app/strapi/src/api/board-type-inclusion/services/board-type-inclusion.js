'use strict';

/**
 * board-type-inclusion service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::board-type-inclusion.board-type-inclusion');
