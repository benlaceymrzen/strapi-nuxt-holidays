'use strict';

/**
 * accommodation-type router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::accommodation-type.accommodation-type');
